import React from "react";

const ResponsePrograms = ({ selectedCard }) => {
  return (
    <div>
      <h1>Selected Card:</h1>
      <p>{selectedCard}</p> 
    </div>
  );
};

export default ResponsePrograms;
